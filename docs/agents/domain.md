# Domain Docs

This is a single-context repository. Before work, read root `CONTEXT.md` if present and relevant ADRs under `docs/adr/` if present. If they do not exist, proceed without creating them.

Use glossary terminology from `CONTEXT.md` when it exists. Surface any conflict with an applicable ADR.
